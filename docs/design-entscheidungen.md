# Design-Entscheidungen

**Quelle der Wahrheit fürs „warum".** Wer wissen will, weshalb etwas so gebaut ist, schaut hier —
und ändert es nicht, ohne die Entscheidung hier zu widerrufen.

## Wann ein Eintrag entsteht

Immer, wenn eine Festlegung getroffen wird, die später jemand hinterfragen könnte:
Technologiewahl, Datenformat, Namensschema, Zuständigkeitsgrenze, bewusst nicht Gebautes,
neue Laufzeit-Abhängigkeit.

**Nicht** eingetragen werden reine Umsetzungsdetails, die der Code selbst zeigt.

## Ablauf

1. Nächste freie `D-xxx` vergeben (fortlaufend, nie wiederverwenden).
2. Zeile in die Tabelle unten eintragen.
3. Bei tragweiter Entscheidung zusätzlich ein ADR anlegen:
   `docs/adr/D-xxx-kurzname.md` auf Basis von [adr/0000-vorlage.md](adr/0000-vorlage.md), und aus
   der Tabelle darauf verlinken.
4. Wird eine Entscheidung später gekippt: alte Zeile auf Status **Ersetzt** setzen und auf die neue
   `D-yyy` verweisen. **Zeilen werden nie gelöscht** — sonst geht die Begründung verloren, warum
   der frühere Weg verworfen wurde.

## Status-Werte

| Status | Bedeutung |
|---|---|
| Aktiv | Gilt und ist umgesetzt |
| Geplant | Beschlossen, aber noch nicht im Code — siehe [roadmap.md](roadmap.md) |
| Ersetzt | Durch eine spätere Entscheidung abgelöst, Verweis in der Begründung |
| Verworfen | Bewusst nicht umgesetzt, Begründung bleibt als Warnung stehen |

## Log

Die Nummern **D-001 bis D-003** stammen aus der Projektvorlage und gelten für jedes Projekt. Die
im [CHANGELOG.md](../CHANGELOG.md) referenzierte **D-033** wurde vor dem Anlegen dieses Logs
vergeben; um keine Nummer doppelt zu belegen, führen neue Einträge den Nummernkreis ab **D-034**
fort.

| ID | Datum | Entscheidung | Status | Begründung / Verweis |
|---|---|---|---|---|
| D-001 | 13.08.2026 | Regeln für KI-Agenten liegen in `AGENTS.md`; `CLAUDE.md`, `GEMINI.md`, `copilot-instructions.md` und `.cursor/rules/` sind reine Verweise darauf | Aktiv | Jede Regel existiert genau einmal. Alternative „je Tool eine eigene Datei" wurde verworfen, weil die Kopien erfahrungsgemäß auseinanderlaufen. |
| D-002 | 13.08.2026 | Datum immer `TT.MM.JJJJ`, Uhrzeit immer Berliner Zeit als `hh:mm` bzw. `hh:mm:ss`, ohne Offset oder Zonenkürzel (eiserne Regel 9 in [`AGENTS.md`](../AGENTS.md)) | Aktiv | Einheitliche Lesart in Doku, Changelog, Logs und UI. Alternative „ISO 8601 mit Offset überall" wurde verworfen: technisch korrekt, für die deutschsprachige Zielgruppe aber unlesbar. Maschinenformate bleiben davon ausgenommen. |
| D-003 | 13.08.2026 | Designsprachen über `data-design` (`ha` = Home Assistant mit `#18BCF2`, `fcr` = FC Ruderting), ohne Default und mit grauem Akzent als sichtbarem „nicht entschieden"; Hell/Dunkel über `data-theme` in jeder Sprache Pflicht (eiserne Regeln 10 und 11 in [`../AGENTS.md`](../AGENTS.md)) | Aktiv | Ein Vokabular, mehrere Akzentsätze. Alternative „je Designsprache eine eigene styles.css" wurde verworfen, weil dann jede Klassenänderung doppelt gepflegt werden müsste. Ein stiller Default wurde ebenfalls verworfen: er hätte fremde Projekte in den Farben eines anderen erscheinen lassen, statt die offene Entscheidung zu zeigen. |
| D-033 | 07.07.2026 | Steuermodus-abhängige Übernahme der Energy-Pilot-Vorschläge: `auto` = KI, `manuell` = normale Regeln, `aus` = aus — global und je Gerät | Aktiv | Vor diesem Log vergeben, siehe [CHANGELOG.md](../CHANGELOG.md). Die technische Freigabe bleibt in jedem Modus hartes Gate; fehlt ein Vorschlag, greift der Nutzerwert, damit ein KI-Ausfall die Anlage nie blockiert. Details: [datenmodell.md](datenmodell.md). |
| D-034 | 13.08.2026 | Deutsche Bezeichner in HA-Helfer-Entitäten und in den davon abgeleiteten Feldern von `/api/status` bleiben unverändert (`prioritat`, `anforderung_current_w`, `schutz_w`, `geschuetzte_mindestleistung_w`) | Aktiv | Sie sind Datenvertrag zu jeder bestehenden Anlage und zum Energy Pilot. Die Alternative „englisch umbenennen, wie es Regel 2 verlangt" wurde verworfen: sie hätte jede installierte Anlage stillgelegt, bis der User sämtliche Helfer neu anlegt. Erweitert wird additiv, umbenannt wird nicht. Neuer Code ist englisch. |
| D-035 | 13.08.2026 | Das gebaute SPA-Bundle liegt eingecheckt unter `app/static/`; die CI baut neu und bricht bei Abweichung ab | Aktiv | Ein HA-Add-on wird auf dem HA-Host gebaut, wo für `i386` und `armhf` kein Node.js-Image existiert und ein Build auf einem Raspberry Pi Minuten dauert. Ausführlich: [adr/D-035-spa-bundle-eingecheckt.md](adr/D-035-spa-bundle-eingecheckt.md). |
| D-036 | 13.08.2026 | Unter HA-Ingress: `base: './'`, API-Pfade ohne führenden Slash, `HashRouter` statt `BrowserRouter` | Aktiv | Der Ingress-Pfad entsteht erst zur Laufzeit. Ausführlich: [adr/D-036-ingress-routing.md](adr/D-036-ingress-routing.md). |
| D-038 | 13.08.2026 | Die Referenz-`styles.css` wird um einen HEMS-Block erweitert (`.device-grid`, `.device-card`, `.kv-row`, `.ctrl-row`, `.pill-row`, `.section-title`), und die Schalter-Checkbox liegt unsichtbar über dem Track statt auf `display: none` | Aktiv | Für Kennzahlzeilen und das Geräteraster gibt es im Katalog keine Entsprechung, und jede neue Klasse wird von mindestens zwei Seiten benutzt. Die Schalter-Änderung ist eine Korrektur: `display: none` nimmt die Checkbox aus der Tab-Reihenfolge, was der Barrierefreiheits-Abschnitt in [design-system.md](design-system.md) ausschließt. |
| D-037 | 13.08.2026 | Für Menschen lesbare Zeitangaben liefert `/api/status` **zusätzlich** als `last_cycle_at` (`TT.MM.JJJJ hh:mm:ss`, Berliner Zeit); das Maschinenformat bleibt in `last_cycle_at_iso` und `status.timestamp` erhalten | Aktiv | Regel 9 verlangt das deutsche Format in der Anzeige, D-034 verbietet das Brechen bestehender Verbraucher. Die Alternative „Format an Ort und Stelle ändern" wurde verworfen, weil der Energy Pilot `last_cycle_at` spiegelt. Additiv erfüllt beides. |
| D-039 | 18.08.2026 | HEMS veröffentlicht einen additiven, semantischen Gerätevertrag und akzeptiert EP-Vorschläge nur über einen passenden, noch gültigen `sensor.ep_plan_commit` | Aktiv | HEMS bleibt autoritative Quelle für Geräte, Userwerte und Grenzen. Stabile Schlüssel/Rollen verhindern Suffix-Raten und das Integrieren technischer Grenzwerte als Verbrauch. Der Commit-Marker verhindert gemischte Teilpläne; bei jedem Fehler greift feldweise der Nutzerwert. Die Modusachse bleibt unverändert. Ausführlich: [adr/D-039-energy-pilot-planvertrag.md](adr/D-039-energy-pilot-planvertrag.md). |
| D-040 | 20.08.2026 | AC-gekoppelte Speicher als eigene Geräteklasse `BatteryDevice`: der Pool wird um die gemessene Entladung bereinigt, Laden und Entladen haben getrennte Prioritäten, und die Ausgabe ist ein signierter Sollwert plus Betriebsart | Aktiv, in Teilen ersetzt durch D-041, D-043 und D-044 | Ein Speicher ist der erste Teilnehmer, der den Messwert verfälscht, auf dem das HEMS aufbaut. Die Alternative „Speicher regelt selbst" wurde verworfen: zwei autonome Regler am selben Netzpunkt haben kein eindeutiges Gleichgewicht. Ohne konfigurierten Speicher bleibt das Verhalten bit-identisch. Ausführlich: [adr/D-040-ac-speicher.md](adr/D-040-ac-speicher.md). **Geändert durch D-041:** SoC-Taper, Notstromreserve und die Entlade-Sofort-Schwelle sind entfallen. **Geändert durch D-043:** Die physische Grenze kommt aus zwei direkten Wattwerten in der Add-on-Konfiguration. **Geändert durch D-044:** `hausdefizit_w` stammt für AC-Speicher aus der separaten Hausleistungsbilanz, nicht aus der negativen Hälfte des Überschuss-Sensors. |
| D-041 | 21.08.2026 | Die physische Grenze eines AC-Speichers kommt allein aus `available_charge_power_entity` und `available_discharge_power_entity`; beide sind Pflicht und werden getrennt ausgewertet. SoC-Taper, Notstromreserve, konfigurierte Maximalleistungen, Entlade-Sofort-Schwelle und die EP-Maximalvorschläge entfallen ersatzlos | In Teilen ersetzt durch D-043 | Sieben Mechanismen begrenzten dieselbe Größe. D-043 ersetzt ausschließlich die beiden Sensorfelder durch direkte Wattwerte; die übrigen Vereinfachungen bleiben aktiv. Ausführlich: [adr/D-041-speichervertrag-vereinfacht.md](adr/D-041-speichervertrag-vereinfacht.md). |
| D-042 | 21.08.2026 | Die Add-on-Optionen werden mit `hassio_role: manager` über die Supervisor-API (`addons/self/options`) verwaltet; `/data/options.json` wird nur gelesen. Schutz gegen Paralleländerung über einen Revisions-Hash der rohen Optionen, Speichern und Neustarten bleiben getrennt | Aktiv | Es gibt keine zweite Konfigurationsquelle und keine eigene Persistenz. Die Default-Rolle reicht nur zum Lesen und führte beim realen Schreiben zu `HTTP 403`; die Admin-Rolle wäre unnötig weitreichend. Die Alternative „eigene Datei unter /data/" wurde verworfen, weil dann zwei Quellen existierten und die Startreihenfolge entschiede; „options.json direkt beschreiben" wurde verworfen, weil der Supervisor die Datei besitzt und beim nächsten Optionswechsel überschreibt. Ausführlich: [adr/D-042-supervisor-konfigurationsseite.md](adr/D-042-supervisor-konfigurationsseite.md). |
| D-043 | 21.08.2026 | Die verfügbare Lade- und Entladeleistung eines AC-Speichers wird als `available_charge_power_w` und `available_discharge_power_w` direkt in Watt gepflegt, nicht als HA-Sensorzuordnung | Aktiv | Der reale Gerätevertrag liefert hier Konfigurationswerte statt laufender Sensoren. `0` sperrt nur die jeweilige Richtung; fehlende oder ungültige Werte machen den Speicher inaktiv. Ausführlich: [adr/D-043-speichergrenzen-als-wattwerte.md](adr/D-043-speichergrenzen-als-wattwerte.md). |
| D-044 | 21.08.2026 | AC-Speicher entladen nach einer separaten signierten Hausleistungsbilanz (`battery_residual_power_entity`), die Netz- und E3DC-Batterieleistung zusammenführt; der Überschuss-Sensor bleibt für PV-Laden und Verbraucher | Aktiv, ergänzt durch D-045 | Der konservative Überschuss-Sensor würde an der Nulleinspeisung um bis zu `700 W` vorbeiregeln. Ein einziger zusätzlicher Bilanzsensor bildet die Unterdeckung ab, ohne einen zweiten Roh-Netzschutz einzuführen. Ausführlich: [adr/D-044-hausleistungsbilanz-ac-speicher.md](adr/D-044-hausleistungsbilanz-ac-speicher.md). |
| D-045 | 22.08.2026 | Überschuss und Hausleistungsbilanz akzeptieren zusätzlich eine im Ingress-Panel gepflegte Formel: benannte HA-Entitäten plus ein eingeschränkter, AST-Whitelist-geprüfter Python-Ausdruck, der `ueberschuss` bzw. `hausbilanz` berechnet. Ein gültiges Ergebnis ersetzt die konfigurierte Einzel-Entität vollständig; leerer oder ungültiger Code lässt das Verhalten unverändert | Aktiv | Der Regelzyklus läuft synchron in einem einzigen asyncio-Prozess — hängender Code blockierte dort auch die Notabschaltung. Ein eigener, schleifenfreier Interpreter ist strukturell nie blockierend und braucht keinen Timeout. Ergänzt D-044s externe HA-Vorlage um eine im Panel editier- und testbare Alternative, ersetzt sie nicht. Ausführlich: [adr/D-045-formel-basierte-sensorwerte.md](adr/D-045-formel-basierte-sensorwerte.md). |
| D-046 | 23.08.2026 | Anzeigedaten für die Skytech Power Flow Card werden als zwei eigene `sensor.*`-Entitäten über `POST /api/states` veröffentlicht, nicht über Ingress abgefragt. Sie tragen ausschließlich Verweise auf HA-Entitäten und Kennzahlen, schalten nichts und laufen nach dem Zyklus statt darin | Aktiv | Eine Lovelace-Karte läuft im HA-Frontend, nicht unter `/api/hassio_ingress/<token>/`. Die Alternative „Karte ruft das Add-on über Ingress" wurde verworfen: sie verlangt eine per WebSocket beschaffte Ingress-URL samt Session-Cookie, ist nur für Administratoren möglich, hängt an HA-Interna und braucht Polling. Der Entitätsweg ist HA-nativ, für jeden Benutzer sichtbar und ereignisgesteuert. Preis: Invariante 4 gilt fortan ausdrücklich nur für den Regelpfad, und per `POST /api/states` erzeugte Entitäten überleben keinen HA-Neustart — der Publisher erkennt das am fehlenden Eintrag im Zustandsabbild. Ausführlich: [adr/D-046-flow-card-veroeffentlichung.md](adr/D-046-flow-card-veroeffentlichung.md). |
| D-047 | 23.08.2026 | Der Datenvertrag zur Power Flow Card trägt eine ganzzahlige `schema_version` und wächst additiv: neue optionale Felder und neue `power_kind`-Werte erhöhen sie nicht, erhöht wird nur, wenn ein Feld entfällt, umbenannt wird oder seine Bedeutung ändert | Aktiv | Zwei Repositories mit unabhängiger Versionierung brauchen genau eine Kopplung, sonst laufen sie auseinander. Die Alternative „Kartenversion an Add-on-Version binden" wurde verworfen: sie erzwingt Gleichschritt-Releases für Änderungen, die den Vertrag gar nicht berühren. Die Karte ignoriert, was sie nicht kennt, und verweigert die Zeichnung nur bei einer höheren `schema_version` als der unterstützten. Der Vertrag liegt wortgleich in beiden Repos unter `vertrag_powerflow_card_hems/kontrakt.md` und hat dort höhere Autorität als jede lokale Beschreibung. |
| D-049 | 27.08.2026 | Die Liste der Dashboards und Ansichten holt das Add-on über **WebSocket** (`lovelace/dashboards/list`, `lovelace/config`) und reicht sie über `GET api/flow/dashboards` an das Panel weiter; je Knoten der Flow Card lässt sich daraus ein Navigationsziel wählen | Aktiv | Die Lovelace-Konfiguration gibt Home Assistant ausschließlich über WebSocket heraus — einen REST-Endpunkt dafür gibt es nicht. Damit spricht das Add-on erstmals ein zweites Protokoll mit HA. Die Alternative „Freitextfeld für den Pfad" wurde verworfen: der Benutzer müsste die Pfade kennen, und ein Tippfehler fällt erst beim Klick auf der Karte auf. Die Alternative „Auswahl im Lovelace-Editor der Karte" wurde ebenfalls verworfen — dort wäre die Liste ohne Umweg verfügbar, aber die Ziele wären je Karte statt einmal zentral gepflegt, und die Karte soll ohne Dashboard-Konfiguration auskommen (D-046). Preis: eine zweite Schnittstelle, die ausfallen kann. Sie liegt außerhalb des Regelpfads, wirft nie, und die Seite fällt auf ein Textfeld zurück. Strategie- und YAML-Dashboards liefern keine Ansichtsliste und erscheinen mit einer Warnung. |
| D-050 | 28.08.2026 | `inactive_reasons` im Kartenvertrag ist **nach Handlungsrelevanz sortiert**: die Bedienfreigabe steht vor der technischen Freigabe, und die Karte zeigt nur den ersten Eintrag | Aktiv | Gemeldet vom Nutzer: unter jedem gesperrten Überschussverbraucher stand immer „Technische Freigabe aus", auch wenn nur die Bedienfreigabe fehlte. Ursache war ein Typfehler in `flow_publisher._inactive_reasons()` — es verglich den **Auflösungszustand** einer Entität (`valid`, `missing`, `invalid`, `unavailable`) mit `"on"`. Der ist nie `on`, also wurden stets beide Gründe angehängt. Die echten Schalterwerte standen im Status gar nicht; `_device_eligible()` las sie und warf sie weg. Sie werden jetzt als **wirksame** Entscheidung gemerkt — unter Energy Pilot schlägt der Vorschlag den Nutzerschalter, und der Grund muss nennen, was tatsächlich gesperrt hat. Die Reihenfolge ist eine bewusste Wahl des Nutzers: sind beide Freigaben aus, nennt die Karte die Bedienfreigabe, weil das der Schalter ist, den er selbst umlegt. Die Alternative „beide Zeilen zeigen" wurde verworfen — sie macht die Karte höher, ohne die Frage „was muss ich tun?" besser zu beantworten. Verworfen wurde ebenso, fehlende von ausgeschalteten Schaltern zu unterscheiden: ein fehlender Schalter wirkt wie ein ausgeschalteter und heißt deshalb auch so. |
| D-048 | 23.08.2026 | Die Anzeigefelder `flow_show`, `flow_icon` und `flow_color` sind vom Geräte-Diff der sicheren Abschaltung ausgenommen | Aktiv | `devices_needing_shutdown()` vergleicht ganze normalisierte Geräte-Dicts. Ohne Ausnahme schaltete ein geändertes Icon den Heizstab vor dem Neustart sicher ab — eine Anzeigeeinstellung griffe damit in den Regelpfad ein, genau das schließt D-046 aus. Die Alternative „Anzeigefelder in eine eigene globale Liste" wurde verworfen: die Zuordnung überlebte ein Umsortieren der Geräteliste dann nur noch über den Namen, und die Felder gehörten sichtbar nicht mehr zum Gerät. |
| D-051 | 03.09.2026 | Der Geltungsbereich geschützter Mindestleistungen ist über `protected_minimum_scope` auswählbar: Bestandsmodus nur gegen Binärgeräte, optional zusätzlich innerhalb der regelbaren Prioritätskaskade | Aktiv | Der neue Modus schützt Sockel regelbarer Verbraucher vor Zusatzleistung höher priorisierter Geräte, ohne Bestandsanlagen umzudeuten. Ausführlich: [D-051](adr/D-051-geschuetzte-mindestleistung-kaskade.md). |
| D-052 | 04.09.2026 | Neue Flow-Card-Option `flow_freigabe_ring_farbe` (Text, Default leer) steuert die Farbe des Freigabe-Rings der Karte je Anlage; additiv im Vertrag als `anzeige.freigabe_ring_farbe`, `schema_version` bleibt `1` | Aktiv | Nutzerwunsch: Der Ring um ein freigegebenes Gerät war fest türkis und sollte weiß werden, dazu im Panel unter „Flow Card" einstellbar statt fest im Karten-Code. Wie `flow_color` je Gerät ein reines Textfeld ohne Formprüfung — ein ungültiger CSS-Wert bleibt Sache des Browsers, nicht des Add-ons. Steht bewusst **nicht** in `GLOBAL_KEYS_FORCING_SHUTDOWN`: eine Anzeigeeinstellung darf keinen Verbraucher abschalten, siehe D-046 und D-048. |
